<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.movierec.recsys.mapper.GenreMapper">

    <select id="findAllGenreNames" resultType="java.lang.String">
        SELECT name FROM genres ORDER BY name;
    </select>

</mapper>